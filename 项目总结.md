# C++ 学习项目总结文档

> **项目路径：** `D:\ware\vsproject\`
> **开发环境：** C++ 语言，Visual Studio IDE，MSVC 编译器
> **生成时间：** 2026-06-27

---

## 项目概览

本项目是一个 **C++ 系统学习项目**，涵盖从基础入门到面向对象进阶的全部内容，共包含 **4 个模块、182+ 个源文件**。

| 模块 | 目录 | 章节数 | 源文件数 | 难度 |
|------|------|--------|----------|------|
| C++入门 | `C++入门/` | 9 章 | ~62 个 .cpp | ⭐ 入门 |
| C++核心编程 | `C++核心编程/` | 5 章（含 7 个子模块） | ~83 个 .cpp/.h | ⭐⭐ 进阶 |
| C++提高编程 | `C++提高编程/` | 3 章 | ~37 个 .cpp/.h/.hpp | ⭐⭐⭐ 高级 |
| 职工管理系统 | `职工管理系统/` | 1 个项目 | ~10 个 .cpp/.h | ⭐⭐ 综合实战 |

---

## 目录

- [第一部分：C++入门](#第一部分c入门)
  - [01-HelloWorld](#01-helloworld基础入门)
  - [02-数据类型](#02-数据类型)
  - [03-运算符](#03-运算符)
  - [04-程序流程结构](#04-程序流程结构程序流程控制)
  - [05-数组](#05-数组)
  - [06-函数](#06-函数)
  - [07-指针](#07-指针)
  - [08-结构体](#08-结构体)
  - [09-通讯录管理系统](#09-通讯录管理系统综合项目)
- [第二部分：C++核心编程](#第二部分c核心编程)
  - [01-内存分区模型](#一01内存分区模型)
  - [02-引用](#二02-引用)
  - [03-函数提高](#三03函数提高)
  - [04-类和对象](#四04类和对象)
  - [05-文件操作](#五05文件操作)
- [第三部分：C++提高编程](#第三部分c提高编程)
  - [01-模板](#一01模板模板编程)
  - [02-STL初识](#二02stlstl入门)
  - [03-STL常用容器](#三03stl常用容器stl常用容器详解)
- [第四部分：职工管理系统](#第四部分职工管理系统基于多态)
- [项目配置](#项目配置)

---

# 第一部分：C++入门

## 01-HelloWorld（基础入门）

**主题：** C++程序的基本结构、注释、变量、常量与标识符命名规则。

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01 c++书写hello world.cpp` | 第一个C++程序 | `#include <iostream>`、`using namespace std`、`cout`、`endl`、`system("pause")`、`main` 函数 |
| `02 c++ 注释.cpp` | 注释的使用 | 单行注释 `//`、多行注释 `/* */` |
| `03 C++变量.cpp` | 变量的创建与赋值 | `数据类型 变量名 = 初始值;` |
| `04 C++常量.cpp` | 两种常量定义 | `#define` 宏常量（预处理替换）、`const` 修饰变量（编译检查） |
| `05 C++关键字、标识符.cpp` | 关键字与标识符 | 关键字不能用作变量名 |
| `06 C++标识符命名规则.cpp` | 命名规则 | 字母/数字/下划线组成、不能以数字开头、区分大小写、见名知意 |

---

## 02-数据类型

**主题：** C++内置数据类型的定义、占用内存大小及输入输出。

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01 C++整型.cpp` | 四种整型 | `short`(2B)、`int`(4B)、`long`(4B)、`long long`(8B)；整型溢出 |
| `02 C++中sizeof关键字.cpp` | 查看内存占用 | `sizeof(数据类型/变量)` |
| `03 C++浮点型.cpp` | 浮点数 | `float`(4B, 6-7位有效数字)、`double`(8B, 15-16位)；科学计数法 `3e2` |
| `04 C++字符型.cpp` | 字符型与ASCII | `char` 占1字节，单引号创建；`(int)ch` 查看ASCII码 |
| `05 C++转义字符.cpp` | 转义字符 | `\n` 换行、`\\` 反斜杠、`\t` 制表符 |
| `06 C++字符串型.cpp` | 字符串类型 | C风格 `char str[]`、C++风格 `string str`（需 `<string>`） |
| `07 C++布尔数据类型.cpp` | 布尔类型 | `bool` 占1字节，`true`=1，`false`=0 |
| `08 C++数据类型输入.cpp` | 键盘输入 | `cin >>` 语法，各种类型输入演示 |

---

## 03-运算符

**主题：** C++四类基本运算符。

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01 C++算术运算符.cpp` | 加减乘除取模 | 整数除法截断、前置 `++a` vs 后置 `a++` |
| `02 C++赋值运算符.cpp` | 复合赋值 | `+=`、`-=`、`*=`、`/=`、`%=` |
| `03 C++比较运算符.cpp` | 六种比较 | `==`、`!=`、`>`、`<`、`>=`、`<=` |
| `04 C++逻辑运算符.cpp` | 逻辑运算 | `!` 非、`&&` 与、`||` 或；非零值为真 |

---

## 04-程序流程结构（程序流程控制）

**主题：** 选择结构、循环结构、跳转语句。

### 选择结构

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01.11 C++选择结构if语句.cpp` | if/else/嵌套if | 高考分数分档案例、嵌套多层判断 |
| `01.11C++选择结构案例.cpp` | 三数比大小 | 嵌套 if-else 找最大值 |
| `01.12 C++选择结构三目运算符.cpp` | 三目运算符 | `条件 ? 值1 : 值2`；C++中可作为左值 |
| `01.13C++ 选择结构switch语句.cpp` | switch语句 | 电影评分系统；`case` 需 `break`；只能判断整型/字符型 |

### 循环结构

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `02.1 C++while循环结构.cpp` | while循环 | `while(条件)` 语法 |
| `02.1 C++whilie循环结构案例.cpp` | 猜数字游戏 | C++11随机数 `random_device` + `mt19937` + `uniform_int_distribution` |
| `02.2 C++dowhile循环语句.cpp` | do-while循环 | `do {} while(条件);` 至少执行一次 |
| `02.2 C++dowhile循环语句水仙花数案例.cpp` | 水仙花数 | 三位数各位立方和等于原数；`%10` 取个位、`/10%10` 取十位、`/100` 取百位 |
| `02.3 C++for循环语句.cpp` | for循环 | `for(初始化; 条件; 更新)` |
| `02.3 C++for循环语句案例.cpp` | 敲桌子游戏 | 7的倍数、个位/十位是7时打印"敲桌子" |
| `02.4 C++嵌套循环.cpp` | 嵌套循环 | 外层控制行，内层控制列 |
| `02.4 C++嵌套循环案例.cpp` | 九九乘法表 | 嵌套for循环 + `\t` 对齐 |

### 跳转语句

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `03.1 C++跳转语句.cpp` | break语句 | switch中跳出、循环中退出、嵌套只跳出内层 |
| `03.2 C++continue语句.cpp` | continue语句 | 跳过本次迭代，继续下一次 |
| `03.3 C++goto语句.cpp` | goto语句 | 无条件跳转，不推荐使用 |

---

## 05-数组

**主题：** 一维数组和二维数组。

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01 C++数组概述.cpp` | 数组三种定义 | 先定义后赋值、初始化赋值、自动推断长度；连续内存、下标从0开始 |
| `02.1 C++一维数组数组名.cpp` | 数组名用途 | `sizeof(arr)/sizeof(arr[0])` 获取元素个数；数组名即首地址 |
| `02.2 C++一维数组案例.cpp` | 找最值、数组逆序 | 遍历找最大值；双指针首尾交换逆序 |
| `02.3 C++冒泡排序.cpp` | 冒泡排序 | 外层 n-1 轮，内层比较相邻元素并交换；`size-1-i` 优化 |
| `03.1 C++二维数组定义.cpp` | 四种定义方式 | 逐元素赋值、分组初始化、线性初始化、省略行数 |
| `03.2 C++二维数组名.cpp` | 二维数组名 | 行数/列数计算；内存连续存储 |
| `03.3 C++二维数组应用案例.cpp` | 学生成绩统计 | 嵌套for循环计算每人总分 |

---

## 06-函数

**主题：** 函数的定义、调用、传递方式及分文件编写。

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01 C++函数的定义.cpp` | 函数定义 | 五要素：返回类型、函数名、参数列表、函数体、return |
| `02 C++函数的调用.cpp` | 函数调用 | 形参 vs 实参；值传递 |
| `03 C++值传递.cpp` | 值传递 | 形参改变不影响实参 |
| `04 C++函数常见样式.cpp` | 四种样式 | 无参无返、有参无返、无参有返、有参有返 |
| `05 C++函数的声明.cpp` | 函数声明 | 声明可多次，定义只能一次 |
| `06 C++函数分文件编写.cpp` | 分文件编写 | `.h` 头文件声明 + `.cpp` 源文件定义 |
| `swap.h` / `swap.cpp` | 分文件示例 | swap 函数的头文件声明和源文件定义 |

---

## 07-指针

**主题：** 指针的定义、使用、const修饰、与数组和函数的结合。

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01 C++指针的定义和使用.cpp` | 指针创建与解引用 | `数据类型 *p;`、`p = &a;`、`*p` 解引用 |
| `02 C++指针所占内存空间.cpp` | 指针大小 | 32位→4字节，64位→8字节；与指向类型无关 |
| `03 C++空指针和野指针.cpp` | 空指针与野指针 | `NULL` 初始化；野指针指向非法内存，应避免 |
| `04 C++const修饰指针.cpp` | const三种情况 | 常量指针 `const int*`（值不可改）、指针常量 `int* const`（指向不可改）、双重const |
| `05 C++指针和数组.cpp` | 指针遍历数组 | 数组名即首地址；`p++` 偏移一个元素 |
| `06 C++指针和函数.cpp` | 地址传递 | 传指针可修改实参 |
| `07 C++指针、数组、函数.cpp` | 指针冒泡排序 | `bubbleSort(int *arr, int len)` 综合应用 |

---

## 08-结构体

**主题：** 自定义数据类型 struct 的全面使用。

| 文件名 | 演示内容 | 关键知识点 |
|--------|----------|-----------|
| `01 C++结构体定义和使用.cpp` | 定义与创建 | `struct Student {...};` 三种创建方式；`.` 访问成员 |
| `02 C+结构体数组.cpp` | 结构体数组 | 下标访问和修改 |
| `03 C++结构体指针.cpp` | 指针访问 | `->` 运算符通过指针访问成员 |
| `04 C++结构体嵌套结构体.cpp` | 嵌套结构体 | `t.stu.name` 逐层访问 |
| `05 C++结构体做函数参数.cpp` | 函数传参 | 值传递不改实参；地址传递可改实参且节省内存 |
| `06 C++结构体中const使用场景.cpp` | const保护 | `const student *s` 防止函数内误改；推荐指针+const |
| `07 C++结构体案例1.cpp` | 老师带学生 | 结构体嵌套、数组、函数综合运用 |
| `08 C++结构体案例2.cpp` | 英雄排序 | 结构体数组 + 冒泡排序按年龄排序 |

---

## 09-通讯录管理系统（综合项目）

**主题：** 综合运用前述所有知识点的完整项目。

**数据结构：**
- `Person`：姓名、性别、年龄、电话、地址
- `Adressbooks`：Person数组（最大1000人）+ 当前人数

**七大功能模块：**
1. **添加联系人** — 容量检测、性别验证(1/2)、年龄验证(0-120)
2. **显示联系人** — 遍历打印，三目运算符转换性别
3. **查找是否存在** — 按姓名遍历，返回下标或-1
4. **删除联系人** — 查找后数组前移覆盖
5. **查找联系人** — 定位后打印详情
6. **修改联系人** — 逐项重新输入，含验证
7. **清空联系人** — 确认后 `m_Size` 置0

**涉及知识点：** 宏常量、结构体嵌套、函数封装、指针传参、while+switch菜单、cin输入、system调用、字符串比较、输入验证、数组前移删除

---

# 第二部分：C++核心编程

## 一、01内存分区模型

**主题：** C++程序运行时的内存四区——栈区、全局区、堆区。

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++全局区.cpp` | 全局区数据 | 全局变量、静态变量 `static`、字符串常量、全局常量在全局区；局部变量/局部常量在栈区 |
| `02 C++栈区.cpp` | 栈区生命周期 | 编译器自动管理；不要返回局部变量地址（悬空指针） |
| `03 C++堆区.cpp` | 堆区数据 | `new` 开辟堆内存；指针在栈区，数据在堆区；不会自动释放 |
| `04 C++new操作符.cpp` | new/delete | `new int(10)` 创建单个、`new int[10]` 创建数组；`delete p` / `delete[] arr` 释放 |

---

## 二、02 引用

**主题：** C++引用机制——语法、传参、返回值、本质。

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++引用的基本使用.cpp` | 引用语法 | `int& b = a;` 创建别名，修改 b 直接影响 a |
| `02 C++引用注意事项.cpp` | 两条规则 | 必须初始化；初始化后不可更改指向 |
| `03 C++引用做函数参数.cpp` | 三种传递对比 | 值传递、地址传递、引用传递（语法更简洁，效果同地址传递） |
| `04 C++引用做函数返回值.cpp` | 引用返回 | 不要返回局部变量引用；返回静态变量引用安全；可作左值 |
| `05 C++引用的本质.cpp` | 底层原理 | 引用本质是指针常量 `int* const ref = &a` |
| `06 C++常量引用.cpp` | const引用 | `const int& ref = 10;` 编译器自动创建临时变量；修饰函数形参防误改 |

---

## 三、03函数提高

**主题：** 函数高级特性——默认参数、占位参数、函数重载。

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++函数默认参数.cpp` | 默认参数 | 从某位置往后都须有默认值；声明和实现只能一处指定 |
| `02 C++函数占位参数.cpp` | 占位参数 | `void func(int a, int, int=10)` 只有类型无名称 |
| `03.1 C++函数重载.cpp` | 函数重载 | 同作用域、同名、参数个数/类型/顺序不同；返回值不同不算 |
| `03.2 C++函数重载注意事项.cpp` | 重载陷阱 | `int&` 和 `const int&` 可重载；默认参数+重载可能二义性 |

---

## 四、04类和对象

**主题：** 面向对象编程核心，分为 7 个子模块。

### 4.1 封装

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++封装的意义.cpp` | Circle类 | 属性+行为封装在一起 |
| `02 C++封装案例.cpp` | Student类 | 公共接口（setter）为私有属性赋值 |
| `03 C++封装的权限控制.cpp` | 三种权限 | `public`（类内外可访问）、`protected`（类内+子类）、`private`（仅类内） |
| `04 C++中struct和class区别.cpp` | 默认权限 | struct 默认 public，class 默认 private |
| `05 C++成员属性设置为私有.cpp` | getter/setter | 只读/只写/可读可写；setter 加有效性校验 |
| `06 C++封装案例-立方体.cpp` | Cube类 | 成员函数 vs 全局函数两种判断方式；引用传参 |
| `07 C++封装案例-点和圆的关系.cpp` | 多文件编程 | Point/Circle 分文件；`#pragma once`；距离平方判断 |

### 4.2 对象的初始化和清理

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++构造函数和析构函数.cpp` | 构造/析构 | 自动调用、只调用一次；不提供则编译器给空实现 |
| `02 C++构造函数的分类和调用.cpp` | 分类与调用 | 无参/有参/拷贝构造；括号法/显示法/隐式转换法；`Person p()` 陷阱 |
| `03 C++拷贝构造函数调用时机.cpp` | 三种场景 | 对象初始化、值传递传参、值方式返回 |
| `04 C++构造函数调用规则.cpp` | 默认规则 | 有有参→无默认构造；有拷贝→无其他构造 |
| `05 C++深度拷贝和浅拷贝.cpp` | 深浅拷贝 | 浅拷贝→指针共享→重复释放崩溃；深拷贝→new新内存→安全 |
| `06 C++初始化列表.cpp` | 初始化列表 | `: m_A(a), m_B(b), m_C(c) {}` 替代构造体内赋值 |
| `07 C++类对象作为类成员.cpp` | 对象成员 | 先构造成员对象，再构造自身；析构顺序相反 |
| `08 C++静态成员变量.cpp` | 静态变量 | 所有对象共享；编译阶段分配；类内声明、类外初始化 |
| `09 C++静态成员函数.cpp` | 静态函数 | 只能访问静态变量；支持对象/类名两种访问 |

### 4.3 对象模型和this指针

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++成员变量和成员函数分开存储.cpp` | 内存模型 | 非静态变量占对象空间；静态/函数不占；空对象1字节 |
| `02 C++中this指针概念.cpp` | this指针 | 解决同名冲突；`return *this` 实现链式调用 |
| `03 C++空指针访问成员函数.cpp` | 空指针安全 | 可调用不访问成员的函数；需 `if(this==NULL)` 保护 |
| `04 C++const修饰成员函数.cpp` | 常函数/常对象 | `const` 修饰后 this 不可改值；`mutable` 可突破限制 |

### 4.4 友元

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++全局函数做友元.cpp` | 全局函数友元 | `friend void func(...)` 访问 private |
| `02 C++类做友元.cpp` | 类友元 | `friend class GoodGay;` 整个类可访问 |
| `03 C++成员函数做友元.cpp` | 成员函数友元 | `friend void GoodGay::visit();` 精细控制 |

### 4.5 运算符重载

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++加号运算符重载.cpp` | `+` 重载 | 全局函数/成员函数两种方式；可重载多种参数组合 |
| `02 C++左移运算符重载.cpp` | `<<` 重载 | 全局函数重载（cout在左）；返回 `ostream&` 支持链式；需友元 |
| `03 C++递增运算符重载.cpp` | `++` 重载 | 前置返回引用，后置返回值（int占位参数区分） |
| `03.1 C++递减运算符重载.cpp` | `--` 重载 | 同递增，前置/后置 |
| `04 C++赋值运算符重载.cpp` | `=` 重载 | 深拷贝防重复释放；返回 `*this` 引用支持链式赋值 |
| `05 C++关系运算符重载.cpp` | `==`/`!=` 重载 | 比较成员变量判断相等 |
| `06 C++函数调用运算符重载.cpp` | `()` 重载（仿函数） | 对象可像函数一样调用；匿名对象 `MyAdd()(100,100)` |

### 4.6 继承

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++继承的基本语法.cpp` | 继承语法 | `class 子类 : 继承方式 父类`；减少重复代码 |
| `02 C++继承方式.cpp` | 三种继承方式 | public/protected/private 对成员权限的影响 |
| `03 C++继承中的对象模型.cpp` | 对象模型 | 所有非静态成员都被继承（private被隐藏）；`cl /d1` 查看 |
| `04 C++继承中构造和析构顺序.cpp` | 构造析构顺序 | 先父后子构造，先子后父析构 |
| `05 C++继承同名成员处理方式.cpp` | 同名成员 | 子类默认访问自己；`Base::m_A` 访问父类；子类函数隐藏父类所有同名函数 |
| `06 C++继承同名静态成员处理方法.cpp` | 同名静态 | 支持对象/类名两种方式 `Son::Base::m_A` |
| `07 C++多继承语法.cpp` | 多继承 | `class Son : public Base1, public Base2`；同名需作用域区分 |
| `08 C++菱形继承.cpp` | 菱形继承 | 数据两份副本；虚继承 `virtual` 解决；`vbptr` 间接访问 |

### 4.7 多态

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++多态基本概念.cpp` | 多态概念 | 静态多态（重载）vs 动态多态（虚函数）；`vfptr` 虚函数表指针 |
| `02 C++多态案例-计算器类.cpp` | 计算器案例 | 普通写法违反开闭原则；多态写法扩展只需新增子类 |
| `03 C++纯虚函数和抽象类.cpp` | 纯虚函数 | `virtual void func() = 0;`；抽象类无法实例化 |
| `04 C++多态案例二-制作饮品.cpp` | 饮品案例 | 模板方法模式；纯虚函数定义流程步骤 |
| `05 C++虚析构和纯虚析构.cpp` | 虚析构 | 父类指针delete只调父类析构→子类内存泄漏；虚析构/纯虚析构解决 |
| `06 C++多态案例三-电脑组装.cpp` | 电脑组装 | CPU/GPU/Memory 抽象类；依赖倒转原则；虚析构确保正确释放 |

---

## 五、05文件操作

**主题：** 文本文件和二进制文件的读写。

### 5.1 文本文件

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++写文件.cpp` | 文本写入 | `<fstream>` → `ofstream` → `open()` → `<<` 写入 → `close()` |
| `02 C++读文件.cpp` | 文本读取 | `ifstream` → `is_open()` 判断 → 四种读取方式（`>>`/`getline(buf)`/`getline(string)`/`get()`） |

### 5.2 二进制文件

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++读文件.cpp` | 二进制读取 | `ios::in | ios::binary`；`ifs.read((char*)&p, sizeof(Person))` |
| `02 C++写文件.cpp` | 二进制写入 | `ofs.write((const char*)&p, sizeof(Person))`；适合结构体持久化 |

---

# 第三部分：C++提高编程

## 一、01模板（模板编程）

### 1.1 函数模板（7个文件）

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++函数模板语法.cpp` | 模板基本语法 | `template<typename T>`；自动类型推导 vs 显式指定类型 |
| `02 C++函数模板注意事项.cpp` | 注意事项 | 推导必须一致；无参数时必须显式指定 |
| `03 C++函数模板案例.cpp` | 通用排序 | 模板实现选择排序；通用打印函数 |
| `04 C++普通函数和函数模板的区别.cpp` | 隐式转换 | 普通函数可隐式转换；自动推导不可；显式指定可 |
| `05 C++普通函数和函数模板调用规则.cpp` | 调用优先级 | 优先普通函数；`<>` 强制调模板；模板匹配更好时优先模板 |
| `06 C++模板局限性.cpp` | 模板特化 | `template<>` 为特定类型提供专门实现 |

### 1.2 类模板（13个文件）

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++类模板语法.cpp` | 类模板语法 | `template<class T1, class T2>` 多类型参数 |
| `02 C++类模板和函数模板区别.cpp` | 区别 | 类模板无自动推导（C++17前）；可有默认模板参数 |
| `03 C++类模板中成员函数创建时机.cpp` | 延迟实例化 | 成员函数调用时才创建，未调用不编译检查 |
| `04 C++类模板对象做函数参数.cpp` | 三种传参 | 指定类型、参数模板化、整个类模板化 |
| `05 C++类模板与继承.cpp` | 模板继承 | 子类必须指定基类模板T类型 |
| `06 C++类模板成员函数类外实现.cpp` | 类外实现 | 重新声明模板参数；`Person6<T1,T2>::` 作用域 |
| `07 C++类模板分文件编写.cpp` | 分文件编写 | `.h`+.cpp 链接错误；推荐 `.hpp` 合并文件 |
| `08 C++类模板与友元.cpp` | 模板友元 | 类内实现/类外实现（需前向声明+`<>`） |
| `09 C++类模板案例-数组类封装.cpp` | 数组类封装 | `MyArray.hpp`：深拷贝、Push_Back、Pop_Back、operator[] |

---

## 二、02STL（STL入门）

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++Vector存放内置数据类型.cpp` | vector基本使用 | `push_back()`、迭代器、`for_each` 算法 |
| `02 C++Vector存放自定义数据类型.cpp` | 自定义类型 | `vector<Person>` 存对象；`vector<Person*>` 存指针 |
| `03 C++Vector容器嵌套容器.cpp` | 嵌套容器 | `vector<vector<int>>` 模拟二维数组；双层迭代器遍历 |

---

## 三、03STL常用容器

### 01 string容器（8个文件）

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++string容器-构造函数.cpp` | 四种构造 | 默认、char*、拷贝、n个字符 |
| `02 C++string容器-赋值操作.cpp` | 七种赋值 | `operator=` 和 `assign()` |
| `03 C++string容器-字符串拼接.cpp` | 拼接操作 | `operator+=` 和 `append()` |
| `04 C++string容器-查找和替换.cpp` | 查找替换 | `find()` 正向、`rfind()` 反向、`replace()` |
| `05 C++string容器-字符串比较.cpp` | 字符串比较 | `compare()` 返回0/正/负 |
| `06 C++string容器-字符存取.cpp` | 字符访问 | `str[i]` 无检查、`str.at(i)` 有检查 |
| `07 C++string容器-插入和删除.cpp` | 插入删除 | `insert()`、`erase()` |
| `08 C++string容器-子串.cpp` | 子串截取 | `substr(pos, n)`；邮箱提取用户名案例 |

### 02 vector容器（6个文件）

| 文件 | 演示内容 | 关键知识点 |
|------|----------|-----------|
| `01 C++vector容器-构造函数.cpp` | 四种构造 | 默认、区间（迭代器）、n个elem、拷贝 |
| `02 C++vector容器-赋值操作.cpp` | 三种赋值 | `operator=`、`assign(begin,end)`、`assign(n,elem)` |
| `03 C++vector容器-容量和大小.cpp` | 容量大小 | `empty()`、`capacity()`、`size()`、`resize()` |
| `04 C++vector容器-插入和删除.cpp` | 插入删除 | `push_back`、`pop_back`、`insert`、`erase`、`clear` |
| `05 C++vector容器-数据存取.cpp` | 元素访问 | `v[i]`、`v.at(i)`、`front()`、`back()` |
| `06 C++vector容器-互换容器.cpp` | （未完成） | 空骨架，预留给 `swap()` 教学 |

---

# 第四部分：职工管理系统（基于多态）

**项目路径：** `职工管理系统/基于多态的职工管理系统/`

## 系统架构

采用 **C++ 多态** 设计模式，通过抽象基类 `Worker` 定义统一接口，三个子类分别实现。

```
Worker（抽象基类，纯虚函数）
  ├── Employee（普通员工）
  ├── Manager（经理）
  └── Boss（老板）
```

## 文件清单

| 文件 | 职责 |
|------|------|
| `worker.h` | 抽象基类 Worker：`m_Id`、`m_Name`、`m_DeptId`；纯虚函数 `showInfor()`、`getDeptName()` |
| `employee.h/cpp` | 普通员工类，岗位职责："删除公司所有数据" |
| `Manager.h/cpp` | 经理类，岗位职责："老板汇报任务，下发给普通员工" |
| `boss.h/cpp` | 老板类，岗位职责："管理公司所有事务" |
| `workerManager.h` | 管理类头文件：数据成员、全部功能接口声明 |
| `workManager.cpp` | 管理类实现：所有业务逻辑（核心文件） |
| `职工管理系统.cpp` | 主程序入口：菜单循环 + switch-case 分发 |
| `empFile.txt` | 数据持久化文件（空格分隔：编号 姓名 部门编号） |

## 核心功能

| 功能 | 方法 | 实现要点 |
|------|------|----------|
| 添加职工 | `Add_Emp()` | 动态扩展数组、编号重复校验、按岗位创建子类对象 |
| 显示职工 | `Show_Emp()` | 多态调用 `showInfor()` |
| 删除职工 | `Del_Emp()` | 数组前移覆盖、释放内存 |
| 修改职工 | `Mod_Emp()` | 删除旧对象、创建新对象、编号校验 |
| 查找职工 | `Find_Emp()` | 支持按编号/姓名两种方式 |
| 排序 | `Sort_Emp()` | 选择排序，支持升序/降序 |
| 清空 | `Clean_File()` | 二次确认、`ios::trunc` 截断、释放内存 |

## 设计特点

- **Worker\*\* 双重指针** 动态数组存储职工对象
- **多态调用**：基类指针 `Worker*` 统一管理不同类型的职工
- **文件持久化**：每次修改立即同步写入 `empFile.txt`
- **内存管理**：析构函数逐个 delete + delete[] 防止泄漏

---

# 项目配置

## CppProperties.json

Visual Studio IntelliSense 配置：
- 配置方案：`x86-Debug`
- 编译器：`msvc_x86`（32位 MSVC）
- 包含路径：系统 INCLUDE + 工作区所有子目录
- 预处理器宏：`WIN32`、`_DEBUG`、`UNICODE`、`_UNICODE`
- IntelliSense 模式：`windows-msvc-x86`

## .gitignore

忽略规则：
- VS 工程文件：`.vs/`、`*.sln`、`*.vcxproj`、`*.suo` 等
- 构建输出：`Debug/`、`Release/`、`x64/`、`x86/`、`out/`、`build/`
- VS Code 配置：`.vscode/`
- 编译中间文件：`*.obj`、`*.pch`、`*.pdb`、`*.tlog` 等
- 编译产物：`*.dll`、`*.exe`、`*.lib`
- NuGet：`**/[Pp]ackages/*`
- 系统文件：`Thumbs.db`、`Desktop.ini`、`.DS_Store`

---

# 学习路径总结

```
基础语法 → 数据类型 → 运算符 → 流程控制 → 数组 → 函数 → 指针 → 结构体
    ↓
内存模型 → 引用 → 函数高级 → 类和对象（封装/构造/this/友元/运算符重载/继承/多态）→ 文件操作
    ↓
模板（函数模板/类模板）→ STL（容器/迭代器/算法）
    ↓
综合实战：通讯录管理系统 + 职工管理系统
```

整个项目按照 **由浅入深、循序渐进** 的方式组织，每个文件聚焦一个知识点，注释详尽，适合作为 C++ 系统学习的完整教程。
